name: nuSim_env
channels:
  - conda-forge
  - https://conda.anaconda.org/conda-forge/
  - defaults
dependencies:
  - _libgcc_mutex=0.1=conda_forge
  - pip=20.0.2=py_2
  - python
  - pip:
    - cython<3.0
    - matplotlib
    - scipy
    - torch
    - PyYaml
    - configargparse
    - numpy
    - gym
    - pandas
    - pyquaternion
    - scikit-video
